<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>news</title>
    <style>
        * {
            font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
            font-size: 1em;
        }
        body {
            background-color: #1c1e27; color: #fff;
            margin: 30px;
        }
        a { color: rgb(145, 145, 145); text-overflow: ellipsis; overflow: hidden; }
        hr {
            border: none;
            border-top: 1px solid rgba(145, 145, 145, .2);
        }
        .article {
            display: flex;
            flex-direction: row;
            gap: 10px;
            align-items: center;
        }
        .article-image { flex: 1; max-width: 200px }
        .article-details { flex: 2; }
    </style>
</head>

<body>
    <span id="content">
        {{#each publications}}
        <h1>{{this.name}}</h1>
        <a href="{{this.url}}">{{this.url}}</a>
        <div class="articles">
            {{#each this.articles}}<hr>
            <div class="article">
                {{#if this.image}}
                <img src="https://img.248.no/image.png?w=200&sampling=lanczos&url={{this.image}}" alt="{{this.title}}" class="article-image">
                {{else}}
                <div class="article-image"></div>
                {{/if}}
                <div class="article-details">
                    <h2>{{this.title}}</h2>
                    <a href="{{this.link}}">{{this.link}}</a>
                    <p>{{this.pub_date}}</p>
                </div>
            </div>
            {{/each}}
            {{/each}}
        </div>
    </span>
</body>
</html>
